[core]
    editor = nvim
    excludesfile = ~/.gitignore
[column]
    ui = auto
[merge]
    conflictStyle = diff3
[diff]
    algorithm = histogram
    colorMoved = default
    mnemonicPrefix = true
    renames = true

; The following is for git-delta, install that package to see an effect
; [core]
;   pager = delta
; [interactive]
;   diffFilter = delta --color-only
; [delta]
;   navigate = true # use n and N to move between diff sections
;   side-by-side = true # change to false if working on a small screen